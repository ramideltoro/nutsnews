name: Deploy Vercel Production Release
run-name: Deploy Vercel production ${{ github.event.client_payload.source_commit || inputs.source_commit }}

on:
  repository_dispatch:
    types:
      - nutsnews-vercel-production-release
  workflow_dispatch:
    inputs:
      source_commit:
        description: Full app source commit to deploy.
        required: true
        type: string
      image_digest:
        description: Immutable VPS image digest already applied to production.
        required: true
        type: string
      build_id:
        description: Source workflow build ID already applied to production.
        required: true
        type: string
      vps_apply_run_id:
        description: Protected VPS apply run that authorized this Vercel deploy.
        required: true
        type: string
      release_confirmation:
        description: Type deploy-vercel-production.
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: nutsnews-vercel-production-release
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy exact release to Vercel production
    if: >-
      ${{
        (github.event_name == 'repository_dispatch' && github.event.client_payload.source_repository == 'ramideltoro/nutsnews') ||
        (github.event_name == 'workflow_dispatch' && inputs.release_confirmation == 'deploy-vercel-production')
      }}
    runs-on: ubuntu-latest
    timeout-minutes: 35
    env:
      SOURCE_REPOSITORY: ${{ github.event.client_payload.source_repository || 'ramideltoro/nutsnews' }}
      SOURCE_COMMIT: ${{ github.event.client_payload.source_commit || inputs.source_commit }}
      IMAGE_DIGEST: ${{ github.event.client_payload.image_digest || inputs.image_digest }}
      BUILD_ID: ${{ github.event.client_payload.build_id || inputs.build_id }}
      VPS_APPLY_RUN_ID: ${{ github.event.client_payload.vps_apply_run_id || inputs.vps_apply_run_id }}
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

    steps:
      - name: Validate production release request
        shell: bash
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          set -euo pipefail
          if [[ "$SOURCE_REPOSITORY" != "ramideltoro/nutsnews" ]]; then
            echo "Only ramideltoro/nutsnews may request Vercel production." >&2
            exit 1
          fi
          if [[ ! "$SOURCE_COMMIT" =~ ^[0-9a-f]{40}$ ]]; then
            echo "Source commit must be a full lowercase SHA." >&2
            exit 1
          fi
          if [[ ! "$IMAGE_DIGEST" =~ ^sha256:[0-9a-f]{64}$ ]]; then
            echo "Image digest must be immutable." >&2
            exit 1
          fi
          if [[ ! "$BUILD_ID" =~ ^[1-9][0-9]{0,19}-[1-9][0-9]{0,5}$ ]]; then
            echo "Build ID must be run-attempt." >&2
            exit 1
          fi
          if [[ ! "$VPS_APPLY_RUN_ID" =~ ^[1-9][0-9]{0,19}$ ]]; then
            echo "VPS apply run ID must be numeric." >&2
            exit 1
          fi
          if [[ -z "$VERCEL_TOKEN" || -z "$VERCEL_ORG_ID" || -z "$VERCEL_PROJECT_ID" ]]; then
            echo "VERCEL_TOKEN, VERCEL_ORG_ID, and VERCEL_PROJECT_ID secrets are required." >&2
            exit 1
          fi

      - name: Checkout exact app source commit
        uses: actions/checkout@v5
        with:
          ref: ${{ env.SOURCE_COMMIT }}
          persist-credentials: false

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 26
          cache: npm
          cache-dependency-path: web/package-lock.json

      - name: Build and deploy Vercel production
        id: deploy
        shell: bash
        working-directory: web
        env:
          NUTSNEWS_BUILD_ID: ${{ env.BUILD_ID }}
          NUTSNEWS_DEPLOYMENT_TARGET: vercel-production
          NUTSNEWS_SOURCE_COMMIT: ${{ env.SOURCE_COMMIT }}
          NEXT_PUBLIC_NUTSNEWS_BUILD_ID: ${{ env.BUILD_ID }}
          NEXT_PUBLIC_NUTSNEWS_SOURCE_COMMIT: ${{ env.SOURCE_COMMIT }}
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          set -euo pipefail
          npx --yes vercel@latest pull --yes --environment=production --token "$VERCEL_TOKEN"
          node --input-type=module <<'NODE'
          import { readFile, writeFile } from "node:fs/promises";

          const envPath = ".vercel/.env.production.local";
          const shellSensitiveEnvNames = new Set(["HOME", "PATH", "Path", "SHELL"]);
          try {
            const input = await readFile(envPath, "utf8");
            const lines = input.split(/\r?\n/);
            const safeLines = lines.filter((line) => {
              const match = line.match(/^\s*([A-Za-z_][A-Za-z0-9_]*)=/);
              return !match || !shellSensitiveEnvNames.has(match[1]);
            });
            if (safeLines.length !== lines.length) {
              await writeFile(envPath, `${safeLines.join("\n").replace(/\n*$/, "")}\n`);
              console.log("Removed shell-sensitive Vercel env names before local build.");
            }
          } catch (error) {
            if (error?.code !== "ENOENT") {
              throw error;
            }
          }
          NODE
          npx --yes vercel@latest build --prod --token "$VERCEL_TOKEN"
          deploy_output="$RUNNER_TEMP/vercel-production-deploy.txt"
          npx --yes vercel@latest deploy --prebuilt --prod --token "$VERCEL_TOKEN" > "$deploy_output"
          deployment_url="$(grep -Eo 'https://[^[:space:]]+' "$deploy_output" | tail -n 1)"
          if [[ -z "$deployment_url" ]]; then
            echo "Vercel CLI did not return a deployment URL." >&2
            exit 1
          fi
          printf 'deployment_url=%s\n' "$deployment_url" >> "$GITHUB_OUTPUT"

      - name: Verify Vercel production identity
        shell: bash
        env:
          DEPLOYMENT_URL: ${{ steps.deploy.outputs.deployment_url }}
          VERCEL_AUTOMATION_BYPASS_SECRET: ${{ secrets.VERCEL_AUTOMATION_BYPASS_SECRET }}
        run: |
          set -euo pipefail
          node --input-type=module <<'NODE'
          const expectedCommit = process.env.SOURCE_COMMIT ?? "";
          const expectedBuildId = process.env.BUILD_ID ?? "";
          const bypass = process.env.VERCEL_AUTOMATION_BYPASS_SECRET ?? "";
          const headers = bypass
            ? {
                "x-vercel-protection-bypass": bypass,
                "x-vercel-set-bypass-cookie": "true",
              }
            : {};
          const targets = [
            new URL("/healthz", process.env.DEPLOYMENT_URL).toString(),
            "https://www.nutsnews.com/healthz",
            "https://nutsnews.com/healthz",
          ];
          const deadline = Date.now() + 10 * 60 * 1000;
          async function readHealth(url) {
            const response = await fetch(url, { headers });
            if (!response.ok) {
              throw new Error(`${url} returned HTTP ${response.status}`);
            }
            const payload = await response.json();
            if (payload?.ok !== true) {
              throw new Error(`${url} did not return ok=true`);
            }
            if (payload?.sourceCommit !== expectedCommit) {
              throw new Error(`${url} reports sourceCommit=${payload?.sourceCommit}`);
            }
            if (payload?.buildId !== expectedBuildId && payload?.buildId !== expectedCommit) {
              throw new Error(`${url} reports buildId=${payload?.buildId}`);
            }
            if (payload?.deploymentTarget !== "vercel-production") {
              throw new Error(`${url} reports deploymentTarget=${payload?.deploymentTarget}`);
            }
          }
          for (const target of targets) {
            let lastError;
            while (Date.now() < deadline) {
              try {
                await readHealth(target);
                lastError = undefined;
                break;
              } catch (error) {
                lastError = error;
                await new Promise((resolve) => setTimeout(resolve, 10_000));
              }
            }
            if (lastError) {
              throw lastError;
            }
          }
          NODE

      - name: Summarize Vercel production deployment
        shell: bash
        env:
          DEPLOYMENT_URL: ${{ steps.deploy.outputs.deployment_url }}
        run: |
          {
            echo "## Vercel production release"
            echo
            echo "- Source commit: \`${SOURCE_COMMIT}\`"
            echo "- VPS apply run: ${VPS_APPLY_RUN_ID}"
            echo "- Vercel deployment: ${DEPLOYMENT_URL}"
            echo "- Production aliases verified: \`www.nutsnews.com\`, \`nutsnews.com\`"
          } >> "$GITHUB_STEP_SUMMARY"
